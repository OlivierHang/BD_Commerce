{% extends 'base.html.twig' %}

{% block title %}BD - BD Commerce
{% endblock %}

{% block content %}

	{% for bd in bds %}
		<pre>
        {{ dump(bd) }}
    </pre>
		{# <img src="{{ asset('couv/') }}{{ bd.ref }}"> #}
		<img src="{{ asset('couv/') }}{{ bd.image }}">
	{% endfor %}

{% endblock %}
